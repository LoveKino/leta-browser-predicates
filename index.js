'use strict';

/**
 * exports browser related predicates
 *
 * in browser, global object is window
 *
 * TODO: some alias and simple library
 */
module.exports = {
    window,
    getWindow: () => window
};
